{
  "name": "zeev-sla-blocker",
  "version": "2.0.0",
  "description": "Módulo ES para exibir modal de tarefas de correção com SLA - Agora com Preact!",
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "build:css": "tailwindcss -i ./src/styles.css -o ./dist/styles.css --minify",
    "build:js": "node build.mjs",
    "build": "npm run build:css && npm run build:js",
    "dev:build": "npm run build && echo 'Build concluído! Arquivos em dist/'",
    "clean": "rimraf dist",
    "test": "echo 'Abra test.html no navegador para testar'"
  },
  "keywords": [
    "typescript",
    "tailwind",
    "modal",
    "sla",
    "zeev",
    "preact",
    "jsx",
    "reactive"
  ],
  "author": "Antonio Juevan",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "esbuild": "^0.19.8",
    "javascript-obfuscator": "^4.1.1",
    "rimraf": "^5.0.5",
    "tailwindcss": "^3.3.6",
    "terser": "^5.43.1",
    "typescript": "^5.3.2"
  },
  "dependencies": {
    "@preact/compat": "^18.3.1",
    "htm": "^3.1.1",
    "preact": "^10.26.9"
  }
}
