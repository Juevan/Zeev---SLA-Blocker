{
  "name": "zeev-sla-blocker",
  "version": "1.0.0",
  "description": "Módulo ES para exibir modal de tarefas de correção com SLA",
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "build:css": "tailwindcss -i ./src/styles.css -o ./dist/styles.css --minify",
    "build:js": "node build.mjs",
    "build": "npm run build:css && npm run build:js",
    "dev:build": "npm run build && echo 'Build concluído! Arquivos em dist/'",
    "clean": "rimraf dist"
  },
  "keywords": [
    "typescript",
    "tailwind",
    "modal",
    "sla",
    "zeev"
  ],
  "author": "Antonio Juevan",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "esbuild": "^0.19.8",
    "rimraf": "^5.0.5",
    "tailwindcss": "^3.3.6",
    "typescript": "^5.3.2"
  }
}
